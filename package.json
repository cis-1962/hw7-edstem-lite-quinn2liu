{
  "name": "my-fullstack-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "cookie-session": "^2.1.0",
    "express-session": "^1.18.0",
    "mongoose": "^8.2.2",
    "turbo": "^1.11.2"
  },
  "devDependencies": {
    "@types/express-session": "^1.17.0"
  }
}
